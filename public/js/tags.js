// Récupérer l'élément input text
const tagsInput = document.querySelector('#tags-input');

// Initialiser Tagify sur l'élément input text
const tagify = new Tagify(tagsInput, {
  // Options de Tagify
});
